# Four-repo order (locked 2026-09-20)

Do not run four courses in parallel. Same skeleton folders stay. Only the *attention* order changes.

## Rank

1. JS — language. Jonas Complete JS 2025. Skip modules 01, 04, 06 until a hole shows. Priority notes: fundamentals 2, how JS works, DOM/events, data structures / modern operators, then async when those files exist.
2. DSA — interview board. This chat. NeetCode 150 order. Arrays & Hashing Easy next = Valid Anagram. JS is the implementation language.
3. TS — types on JS you already rebuilt. Max course. After you can explain closure + event loop cold. Do not start generics while JS is still foggy.
4. REACT — product skill. Jonas + Smilga. After JS core + enough TS to type props. Walk Ping Me components instead of redoing “React first look” if the note is already there.

Parked courses: Advanced CSS, Node/Express/Mongo.

## Weekly split while employed

- 3–4 short DSA reps (this chat + `Challenges/`)
- 2 JS rebuilds from rusted topics (write a tiny file in `JS/Exercises` or `JS/Repairs`)
- TS only if that week’s JS file needs a type
- React only as a Ping Me / portfolio walk, not a new tutorial app

## Tracking contract

Each artifact is one of: cold rebuild, spoken score, mistake-log line, repair note.
Scores: blank | got the idea | working | interview-ready.
Mentor writes only to DSA, JS, TS, REACT.
