/*
Prompt:
Given an array of positive integers and a target sum, return the length of the
smallest contiguous subarray whose sum is at least the target. Return 0 if none
exists.

Examples:
minSubarrayLength([2, 3, 1, 2, 4, 3], 7) -> 2
minSubarrayLength([1, 1, 1, 1], 5) -> 0
*/

function minSubarrayLength(numbers, target) {
	throw new Error("Not implemented");
}

module.exports = minSubarrayLength;
